import React from 'react';

const MovieCredits = ({ credits }) => {
  return (
    <div className="movie-credits">
      {/* Here you'd map through credits and create a slider */}
    </div>
  );
};

export default MovieCredits;
